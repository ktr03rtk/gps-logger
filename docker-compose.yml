version: "3"

services:
  gps-logger:
    build: .
    privileged: true
    volumes:
      - .:/opt/gps-logger
    tty: true
    container_name: gps-logger
    network_mode: "host"
    cap_add:
      - ALL
